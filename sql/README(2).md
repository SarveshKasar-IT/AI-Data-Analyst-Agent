# Data

This directory is reserved for project data and data documentation.

The Streamlit application reads analytics results from PostgreSQL rather than directly exposing raw data files.

Do not commit sensitive or private datasets to the repository.
